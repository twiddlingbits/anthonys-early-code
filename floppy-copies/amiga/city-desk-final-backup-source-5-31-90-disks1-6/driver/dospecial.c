/** City Desk Gray Scale conversion driver **/

/** NO Alphanumeric functions implemented **/

#include "exec/types.h"

DoSpecial(command, outputBuffer, vline, currentVMI, crlfFlag, Parms)
char outputBuffer[];
UWORD *command;
BYTE *vline;
BYTE *currentVMI;
BYTE *crlfFlag;
UBYTE Parms[];
{
return(0);
}


Close(ior)
struct printerIO *ior;
{
return(0);
}

